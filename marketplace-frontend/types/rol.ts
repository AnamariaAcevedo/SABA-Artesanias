// Forma de datos del módulo de roles (RolResponseDto), usada para el selector de rol.

export interface Rol {
  id: number;
  nombre: string;
  activo: boolean;
  createdAt: string;
  updatedAt: string;
}
